Implementetion of Naive Bayes, LogisticRegression, MLP, AdaBoost algorithms on IMDB's dataset.
The goal is given the IMDB users' reviews to predict if each one of them was a positive or negative one.
Find the dataset here https://keras.io/api/datasets/imdb/
